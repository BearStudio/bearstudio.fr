---
import { cn } from '@/lib/tailwind/utils';

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div
  class={cn(
    'relative mx-auto w-full flex flex-col max-w-5xl px-6 py-12 gap-4',
    className
  )}
>
  <slot />
</div>
