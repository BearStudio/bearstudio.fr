---
import Page, { generateStaticPaths } from '@/pages/fr/equipe/[id]/index.astro';

export async function getStaticPaths() {
  return generateStaticPaths({ locale: 'en' });
}
---

<Page {...Astro.props} />
