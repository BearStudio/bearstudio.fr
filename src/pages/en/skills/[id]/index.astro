---
import Page, {
  generateStaticPaths,
} from '@/pages/fr/expertise/[id]/index.astro';

export async function getStaticPaths() {
  return generateStaticPaths({ locale: 'en' });
}
---

<Page {...Astro.props} />
