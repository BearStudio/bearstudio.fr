---
import Page, {
  generateStaticPaths,
} from '@/pages/fr/blog/articles/[id]/index.astro';

export async function getStaticPaths() {
  return generateStaticPaths({ locale: 'en' });
}
---

<Page {...Astro.props} />
